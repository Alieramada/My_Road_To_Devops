# 4.4 Ваш компьютер в сети

## Ключевые области знаний

* Интернет, сеть, маршрутизаторы
* Запрос конфигурации DNS-клиента
* Запрос конфигурации сети

## Неполный список используемых файлов, терминов и утилит

* `route`, `ip route show`
* `ifconfig`, `ip addr show`
* `netstat`, `ss`
* `/etc/resolv.conf`, `/etc/hosts`
* `IPv4`, `IPv6`
* `ping`
* `host`
