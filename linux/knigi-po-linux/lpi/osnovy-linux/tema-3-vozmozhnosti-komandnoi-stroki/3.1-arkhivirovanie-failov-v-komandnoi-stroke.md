# 3.1 Архивирование файлов в командной строке

## &#x20;Ключевые области знаний

* Файлы, каталоги
* Архивы, сжатие

## &#x20;Неполный список используемых файлов, терминов и утилит

* `tar`
* Общие `tar` параметры
* `gzip`, `bzip2`, `xz`
* `zip`, `unzip`
