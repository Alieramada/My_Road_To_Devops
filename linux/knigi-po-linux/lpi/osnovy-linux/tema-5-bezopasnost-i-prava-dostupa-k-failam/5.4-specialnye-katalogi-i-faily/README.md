# 5.4 Специальные каталоги и файлы

## Ключевые области знаний

* Использование временных файлов и директорий
* Символические ссылки

## Неполный список используемых файлов, терминов и утилит

* `/tmp/`, `/var/tmp/` и липкий бит
* `ls -d`
* `ln -`
